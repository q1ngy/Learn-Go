syntax = "proto3";

package proto;

option go_package = "add_client/proto";

service CalcService {
  rpc Add(AddReq)returns(AddResp){}
}

message AddReq {
  int32 x = 1;
  int32 y = 2;
}

message AddResp {
  int32 result = 1;
}